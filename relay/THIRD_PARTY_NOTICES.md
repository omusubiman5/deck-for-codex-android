# Third-party notices

Runtime dependencies:

- `bonjour-service` — MIT
- `qrcode` — MIT
- `selfsigned` — MIT
- `ws` — MIT

The extracted relay core originates from the MIT-licensed `codex-deck-m18` project. See `SOURCE_PROVENANCE.md` and `LICENSE`.
